# 分离 Git 同步核心与 DataInfra 项目适配器

组合仓安全同步将通用 Git 状态判定与 DataInfra 项目规则分层实现。通用核心负责父仓、gitlink、子仓工作树、开发分支和目标 pin 的确定性状态机；DataInfra 适配器负责运行中实例、受控构建补丁、上游构建入口与安装产物一致性规则。该边界保留 DataInfra 端到端可执行性，同时避免将机器路径和项目例外写入通用同步逻辑，也避免扩展为通用工作流引擎。
