# 组合仓安全同步

本上下文定义开发者本地组合仓版本控制、安全同步与安装产物一致性核验中的核心语言。

## Language

**组合仓**：
通过 Git gitlink 将多个子仓精确版本组合为一个可验证基线的父仓。
_Avoid_: 单仓、普通项目目录

**目标组合**：
上游跟踪分支的某个父仓提交，以及该提交树中记录的全部子仓精确 pin。
_Avoid_: 各子仓最新分支头

**组合仓安全同步**：
在保留可恢复开发工作的条件下，将开发者本地组合仓收敛到目标组合的受控过程。
_Avoid_: 项目管理跟踪、各子仓独立更新

**开发分支**：
子仓中保存开发者工作并可选跟踪远程分支的本地命名引用。
_Avoid_: 组合 pin、detached HEAD

**可自动离开开发状态**：
工作树干净，且开发分支提交已被目标 pin 包含或与目标 pin 的 tree 等价。该状态允许同步流程切换到目标 pin 并保留开发分支引用。
_Avoid_: 可恢复开发状态、已保存、已提交

**项目适配器**：
将通用 Git 组合仓状态机连接到特定项目运行条件、原生构建入口与产物一致性规则的边界。
_Avoid_: 通用同步核心、项目构建系统

**受控构建补丁**：
由父仓版本化保存并由项目原生构建入口幂等应用到子仓工作树的补丁，其当前工作树改动可通过反向应用检查验证。
_Avoid_: 用户本地补丁、构建产物

**受控补丁连续性**：
当前父仓与目标父仓声明相同的受控构建补丁内容、目标子仓和适用路径。该条件允许同步流程暂时反向移除已应用补丁，并在组合 pin 切换后幂等重放。
_Avoid_: 子仓内容未变、补丁仍可应用

**安装身份**：
已安装运行时产物对应的源码版本集合与产物哈希集合。
_Avoid_: 最近构建时间、当前源码版本
